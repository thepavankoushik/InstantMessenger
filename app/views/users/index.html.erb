<h4> List of current users that u can message </h4>
<div align = 'center'>
<ul>
	<% @users.each do |user| %>
 <li>	
	<% if current_user != user %>
		<%= link_to user.email,user_path(user) %>
	<% end %>
</li>
	<% end %>
</ul>
</div>
